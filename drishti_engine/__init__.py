"""Drishti Engine — consumer search and media MCP server.

Provides image generation, stock-template search (images/videos), flight
search, healthcare lookup, and local browser-history search. Real keyless
APIs are used where available with offline mock fallbacks, mirroring the
repo's ``setu_bridge`` and ``veda_engine`` server conventions.
"""

__version__ = "0.1.0"
